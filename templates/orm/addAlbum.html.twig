{% extends 'base.html.twig' %}

{% block menuJ %}
    <script>
        $(document).ready(function () {
            $("a").addClass("yellow");
            $(".addalbum").css("display","none");

        })
    </script>
{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>


    <h3 class="addHeader">Add Album...</h3>

    <div class="row">
        <div class="col-md-6">
            {% if artistId is defined %}
                <a href="{{ path('artistAlbums', { 'artistId': artistId } )}}"><-- Artist Albumss</a>
            {% endif %}
        </div>
        <div class="col-md-6"><a href="#">Add Album --></a></div>
    </div>

    {{ form(albumForm) }}

{% endblock %}
