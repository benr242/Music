{% extends 'base.html.twig' %}

{% block title %}Artists{% endblock %}

{% block menuCss %}
    {{ parent() }}

    #addartist {visibility: visible}
    #addalbum {visibility: visible}
    #addsong {visibility: visible}
{% endblock %}


{% block menuJ %}

{% endblock %}

{% block body %}
    {{ parent() }}

    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <h3 class="addHeader">Artists...</h3>

    Artists: {{ artists | length }}

{#
    <div class="row">
        <div class="col-md-6"><a href="{{ path('showAllArtists') }}"><-- Artists</a> </div>
        <div class="col-md-6"><a href="{{ path('addArtist') }}">Add Artist --></a></div>
    </div>
#}
    <table class="table table-bordered table-striped table-hover albums">
        <tbody>
        {% for artist in artists %}
            <tr class="artist">
                <td>
                    <div class="col-4">{{ artist.id }}</div>
                    <div class="col-7"><a href="{{ path('artistAlbums', {'artistId':artist.id  , 'artistName':artist.name}, relative =  false) }}">{{ artist.name }}</a></div>
                    <div class="col-md-1"><a class="btn btn-outline-danger" href="{{ path('removeArtist', {artistId:artist.id}) }}">-</a></div>
                </td>
            </tr>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {#test: {{ artists[1].name }}#}

{% endblock %}