http://127.0.0.1:8000/orm/showArtistAlbums/152

during 'composer install' (had to delete composer.lock)S
    Package symfony/lts is abandoned, you should avoid using it. Use symfony/flex instead.

ORM
    #edit .env, DATABASE_URL
    $ ./bin/console doctrine:database:create



     * @Route("/orm/showArtistAlbums", name="artistAlbums", defaults={id : 47}}

Bootstrap
Fixtures to add Artists and Album

GIT PUSH
